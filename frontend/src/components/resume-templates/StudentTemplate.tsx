import { TemplateProps, Header, Items, Section } from './template-types';
export function StudentTemplate({ data }: TemplateProps) { return <div className="resume-document template-student"><Header data={data}/>{data.summary && <Section title="Objective"><p>{data.summary}</p></Section>}{data.education.length > 0 && <Section title="Education"><Items items={data.education}/></Section>}{data.projects.length > 0 && <Section title="Projects"><Items items={data.projects}/></Section>}{data.skills.length > 0 && <Section title="Skills"><div className="resume-skills">{data.skills.map(skill => <span key={skill}>{skill}</span>)}</div></Section>}{data.achievements.length > 0 && <Section title="Achievements"><Items items={data.achievements}/></Section>}</div>; }
