import { TemplateProps, Header, Items, Section } from './template-types';
export function CreativeTemplate({ data }: TemplateProps) { return <div className="resume-document template-creative"><div className="creative-banner"><Header data={data}/></div>{data.summary && <Section title="The story"><p>{data.summary}</p></Section>}{data.experience.length > 0 && <Section title="Selected work"><Items items={data.experience}/></Section>}{data.projects.length > 0 && <Section title="Things I've made"><Items items={data.projects}/></Section>}{data.skills.length > 0 && <Section title="Toolkit"><div className="resume-skills">{data.skills.map(skill => <span key={skill}>{skill}</span>)}</div></Section>}</div>; }
