import { TemplateProps, Header, Items, Section } from './template-types';
export function MinimalTemplate({ data }: TemplateProps) { return <div className="resume-document template-minimal"><Header data={data}/>{data.summary && <Section title="Summary"><p>{data.summary}</p></Section>}{data.experience.length > 0 && <Section title="Experience"><Items items={data.experience}/></Section>}{data.education.length > 0 && <Section title="Education"><Items items={data.education}/></Section>}{data.skills.length > 0 && <Section title="Skills"><p>{data.skills.join('  /  ')}</p></Section>}{data.projects.length > 0 && <Section title="Projects"><Items items={data.projects}/></Section>}</div>; }
