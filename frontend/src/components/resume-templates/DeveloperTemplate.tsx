import { TemplateProps, Header, Items, Section } from './template-types';
export function DeveloperTemplate({ data }: TemplateProps) { return <div className="resume-document template-developer"><div className="developer-top"><Header data={data}/></div>{data.summary && <Section title="$ whoami"><p>{data.summary}</p></Section>}{data.skills.length > 0 && <Section title="stack"><div className="resume-skills">{data.skills.map(skill => <span key={skill}>{skill}</span>)}</div></Section>}{data.experience.length > 0 && <Section title="experience"><Items items={data.experience}/></Section>}{data.projects.length > 0 && <Section title="projects"><Items items={data.projects}/></Section>}{data.education.length > 0 && <Section title="education"><Items items={data.education}/></Section>}</div>; }
