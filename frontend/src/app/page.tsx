'use client';
import Link from 'next/link'; import { ArrowUpRight, BarChart3, FileSearch, WandSparkles, PenLine } from 'lucide-react'; import { Footer } from '@/components/Footer'; import { PremiumHome } from '@/components/PremiumHome'; import { useTheme } from '@/components/ThemeProvider';
export function ClassicHome() { const steps = [['01','Upload resume','Bring your existing resume in PDF, DOCX, or image format.'],['02','Rule-based analysis','See the signals recruiters and screening systems notice first.'],['03','Improve resume','Turn focused recommendations into stronger career storytelling.'],['04','Build with confidence','Shape a polished resume from a flexible, professional template.']]; return <main><section className="hero"><div className="hero-copy"><span className="eyebrow">Resume intelligence</span><h1>Make your resume <em>work harder.</em></h1><p>byteforce gives your experience a sharper signal. Analyze what is working, find what is missing, then build a resume that moves you forward.</p><div className="actions"><Link className="button" href="/analyze">Analyze my resume <ArrowUpRight size={17}/></Link><Link className="button secondary" href="/templates">Explore templates</Link></div></div><div className="hero-visual"><div className="visual-board"><div className="visual-top"><b>RESUME INTELLIGENCE</b><span>ready when you are · ●</span></div><div className="visual-main"><div className="glass"><span className="eyebrow">Live analysis</span><h3>Text <span className="accent">in</span><br/>signal</h3><p>Extract structure, spot gaps, and turn your next edit into a deliberate move.</p><div className="progress"><span style={{width:'72%'}} /></div></div><div className="mini-stack"><div className="glass mini-card"><span>ATS structure</span><strong className="accent">scan</strong></div><div className="glass mini-card"><span>Skill coverage</span><strong className="purple">map</strong></div><div className="glass mini-card"><span>Career story</span><strong>shape</strong></div></div></div></div></div></section><section className="page" id="how-it-works"><div className="section-head"><div><span className="eyebrow">A clearer next step</span><h2>From draft to direction.</h2></div><p>One focused workspace for the resume you have and the opportunity you want.</p></div><div className="steps">{steps.map(([num,title,text],i)=><article className="step" key={num}><span className="step-number">{num}</span>{[FileSearch,BarChart3,WandSparkles,PenLine].map((Icon,j)=>j===i&&<Icon key={title} size={25} color={i===1?'#a994ff':'#ff9568'} style={{display:'block',marginTop:28}}/>)}<h3>{title}</h3><p>{text}</p></article>)}</div></section><Footer/></main>; }
export default function Home() { const { theme } = useTheme(); return theme === 'premium' ? <PremiumHome /> : <ClassicHome />; }
