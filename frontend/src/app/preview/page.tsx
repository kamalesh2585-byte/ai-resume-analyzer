'use client';
import { useEffect, useState } from 'react'; import Link from 'next/link'; import { Edit3 } from 'lucide-react'; import { ResumeData, emptyResumeData } from '@/lib/resume-types'; import { TemplateCanvas } from '@/components/resume-templates/TemplateCanvas';
export default function PreviewPage(){const [data,setData]=useState<ResumeData>(emptyResumeData);const [templateId,setTemplateId]=useState('modern');useEffect(()=>{const saved=localStorage.getItem('byteforce-resume-data');const template=localStorage.getItem('byteforce-selected-template');if(saved)setData(JSON.parse(saved) as ResumeData);if(template)setTemplateId(template)},[]);return <main className="page preview-page"><div className="section-head"><div><span className="eyebrow">Final resume</span><h1>Ready to move.</h1><p className="muted">{data.fullName||'Your resume'} · {templateId}</p></div><div className="actions"><Link className="button secondary" href="/builder"><Edit3 size={15}/> Edit resume</Link></div></div><div className="builder-preview final-preview"><TemplateCanvas templateId={templateId} data={data}/></div></main>}
