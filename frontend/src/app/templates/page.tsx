'use client';
import Link from 'next/link'; import { ArrowRight, Eye } from 'lucide-react'; import { demoResumeData } from '@/lib/resume-types'; import { templateRegistry } from '@/components/resume-templates';
export default function TemplatesPage(){return <main className="page"><div className="page-header"><span className="eyebrow">Real React templates</span><h1>Find your format.</h1><p>Every preview below is the same component used in your final resume. Choose one, then replace the preview content with your own information.</p></div><div className="template-grid">{templateRegistry.map(template=>{const Template=template.component;return <article className="template-card" key={template.id}><div className="template-gallery-preview"><Template data={demoResumeData}/></div><div className="template-info"><div><h3>{template.name}</h3><span className="muted">{template.category}</span></div></div><p className="muted">{template.description}</p><div className="template-actions"><Link className="button" href={`/builder?template=${template.id}`}>Use template <ArrowRight size={14}/></Link><Link className="button secondary" href={`/builder?template=${template.id}`}><Eye size={14}/> Live preview</Link></div></article>})}</div></main>}
